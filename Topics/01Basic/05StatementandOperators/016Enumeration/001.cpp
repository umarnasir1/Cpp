/*
  We have an enumeration type called color with 3 enumeration constants: red, green and blue. 

  enum Color { red, green, blue};// we are creating a new type with enumeration constants
  Color screen_color {green}; // create variable of enumeration type Color and initilizing it to green- (can only be red, green, blue)
   // error if we initialize with any other then defined three.  

*/